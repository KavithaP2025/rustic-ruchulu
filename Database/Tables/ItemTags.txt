CREATE TABLE ItemTags (
    ItemTagID INT IDENTITY(1,1) PRIMARY KEY,
    ItemID INT FOREIGN KEY REFERENCES Items(ItemID) ON DELETE CASCADE,
    TagID INT FOREIGN KEY REFERENCES Tags(TagID) ON DELETE CASCADE
);
